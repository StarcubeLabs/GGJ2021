using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Test spawner that causes an enemy to spawn on its location.
/// </summary>
/// 
namespace GGJ2021.Enemy
{
    public sealed class Spawner_Base : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        }
    }
}
